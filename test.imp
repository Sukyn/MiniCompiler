var retour;

function main() {
  var i;
  var arg;
  var j;
  var espace;


  arg = 25;
  i = 0;
  retour = 10;
  espace = 32;
  while (i < arg) {
    j = 0;
    while (j < arg) {
      if (i*i + j*j < arg*arg) {
        putchar(46);
      } else {
        putchar(35);
      }
      putchar(espace);
      j = j+1;
    }
    putchar(print_return(retour));
    i = i + 1;
  }
}

function print_return(x) {
  return x;
}
