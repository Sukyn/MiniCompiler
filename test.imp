var retour;

function main() {
  var i;
  var arg;
  var j;
  var espace;


  arg = 25;
  i = 0;
  retour = 10;
  espace = 32;
  while (i < arg) {
    j = 0;
    while (j < arg) {
      if (i*i + j*j < arg*arg) {
        putchar(46);
      } else {
        putchar(35);
      }
      putchar(espace);
      j = j+1;
      retour = 10;
    }
    putchar(print_return(4, 14, 2, 3, retour, 5, 6));
    i = i + 1;
    espace = 32;
  }
}

function print_return(x, y, z, h , k, r, m) {
  retour = x+y+z;
  if (k < retour+1){
    return k;
    } else { }
}
