var retour;

function main() {
  var i;
  var arg;
  var arg_b;
  var j;
  var espace;

  arg_b = 24;
  arg = arg_b;
  i = 0;
  retour = 10;
  espace = 32;
  while (i < return_arg(arg+1)) {
    j = 0;
    while (j < return_arg(arg+1)) {
      if (i*i + j*j < return_arg(arg+1, 3)*return_arg(arg+1, 4)) {
        putchar(46);
      } else {
        putchar(35);
      }
      putchar(espace);
      j = j+1;
      retour = 10;
    }
    putchar(print_return(4, 14, 2, 3, retour, 5, 6));
    i = i + 1;
    espace = 32;
  }
}

function print_return(x, y, z, h , k, r, m) {
  retour = x+y+z;
  if (k < retour+1){
    return k;
    } else { }
}


function return_arg(arg, n) {
  return arg;
}